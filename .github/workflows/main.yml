name: Github Action Teste

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag value"
        required: true
  release:
    types:
      - published
env:
  TAG: ${{ github.event.release.tag_name || github.event.inputs.tag }}
jobs:
  check-tag:
    runs-on: ubuntu-latest
    steps:
      - name: Check TAG
        if: ${{ !startsWith("$TAG", 'v') }}
        uses: actions/github-script@v6
        with:
          script: |
            core.setFailed('Please, select a valid TAG instead branch [ ${{ github.ref_name }} ]')
      - name: Display env TAG
        run: echo ${{ env.TAG }}

# jobs:
#   job1:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#         with:
#           fetch-depth: 0
#           ref: ${{ github.event.inputs.tag }}
#       - name: Display tag
#         run: echo ${{ github.event.inputs.tag }}
#       - name: Display tag (new version)
#         run: echo ${{ inputs.tag }}
#       - name: Display test.txt
#         run: cat ./test.txt
#       - name: Display env TAG
#         run: echo ${{ env.TAG }}
#   job2:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Display env TAG
#         run: echo ${{ env.TAG }}
  # job3:
  #   runs-on: ubuntu-latest
  #   env:
  #     TAG: ${{ env.TAG }}
  #   steps:
  #     - name: Display env TAG
  #       run: echo ${{ env.TAG }}

