name: Github Action Teste

on:
  workflow_dispatch:

jobs:
  job1:
    needs: job2
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Show message
        run: echo "first step"
  job2:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Show message
        run: echo "second step"
