name: Test
on:
  workflow_dispatch:

jobs:
  show-platform:
    runs-on: ubuntu-latest
    steps:
    - name: setFailed exit code
      uses: actions/github-script@v7
      with:
        script: |
          core.setFailed("This is a failed job generated for Lucas Faria")
